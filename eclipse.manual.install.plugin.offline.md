
```
包含 content.jar and artifacts.jar 的这种，直接在eclipse里点击 路径即可安装。
help->install new software-> add -> archive -> select your zip file

```

```
第二种只有feature、plugin
如果含有feature、plugin
那么这种很明显就是需要解压缩, 然后放到eclipse的安装路径下即可。 
如果已有features、plugins，放在原来的文件夹里 restart eclipse即可。 如果没有记得放在“dropins”下面。
比如我的是：${eclipse_home}/dropins
————————————————
```

```
一般feature里面的xml有下载地址
```